import { assert } from 'chai';

import {
  getDataset
} from '../../common/api/dataset';

describe('Dataset API', function() {
  it('gets a dataset based on id', function() {

    // TODO: fix so that d3.tsv or other loading can work
    // via test.

    // var id = 'receptor_profiling';
    // return getDataset(id).then(function(results) {
    //   assert.isAtLeast(20, results.length);
    // });
  });
});
